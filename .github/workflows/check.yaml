name: Test tool availability

on:
  workflow_dispatch: {}

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test something
        shell: bash
        run: |
          echo -n "Hello" | base64
